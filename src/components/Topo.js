import React from 'react';
import { View, Image } from 'react-native';
import Logo from '../images/jokenpo.png';

const Topo = () => (
  <View>
    <Image source={Logo} />
  </View>
);

export default Topo;
